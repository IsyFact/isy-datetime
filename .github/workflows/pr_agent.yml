name: PR-Agent
on:
  pull_request:
    branches:
      - develop

jobs:
  pr_agent_job:
    uses: IsyFact/isy-github-actions-templates/.github/workflows/pr_agent_template.yml@msg-Tomaszewski-patch-1
    secrets:
      OPENAI_KEY: ${{ secrets.OPENAI_KEY }}
 
